/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.xfoss.learningJava.app;

import com.xfoss.learningJava.list.LinkedList;

import static com.xfoss.learningJava.utilities.StringUtils.join;
import static com.xfoss.learningJava.utilities.StringUtils.split;
import static com.xfoss.learningJava.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
